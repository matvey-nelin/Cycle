[project]
name = "cycle"
version = "1.2.0"
description = "Дневник тренировок и планирования"
dependencies = [
    "flet",
]

[tool.flet.android]
package = "com.nelinmatvey.cycle"
build_number = 120

[tool.flet.ios]
bundle_id = "com.nelinmatvey.cycle"

[tool.flet.macos]
bundle_id = "com.nelinmatvey.cycle"

[tool.flet.windows]
# Автоматически наследует имя, версию и описание из [project]

[tool.flet.linux]
# Автоматически наследует имя, версию и описание из [project]
